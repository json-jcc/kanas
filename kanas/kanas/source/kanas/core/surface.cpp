#include "surface.h"

bool kanas::core::surface::allocate()
{
    VkDisplaySurfaceCreateInfoKHR createInfo;
    createInfo.sType;
    createInfo.pNext;
    createInfo.flags;
    createInfo.displayMode;
    createInfo.planeIndex;
    createInfo.planeStackIndex;
    createInfo.transform;
    createInfo.globalAlpha;
    createInfo.alphaMode;
    createInfo.imageExtent;

    //vkCreateDisplayPlaneSurfaceKHR();

    return false;
}
